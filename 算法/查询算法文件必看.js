/**
 * 发现自己算法差的很，于是恶补一下
 * export import 语法需要在服务器的环境下才能运行
 * VScode下载插件 Live Server 右下角点击Go Live开启服务器
 * 需要注意的地方：
 * 1.这里的<script>标签需要加上type="module" 
 * 2.import {...} from './' 别忘了加.js后缀，不然会404 not found
 */